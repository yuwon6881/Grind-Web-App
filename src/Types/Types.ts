export type User = {
  name: string;
  profilePicture: string;
};

export type Card = {
  date: string;
  note: string;
  duration: number;
  volume: number;
  record: number;
  exercises: string[];
};
